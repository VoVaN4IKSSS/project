# project

# 🧠 Логіка роботи

Цей проєкт побудований на принципі **"Стейт-машини"** — це коли програма знає, на якому екрані вона знаходиться, і що їй робити в конкретний момент.

## 1. Перемикання екранів (Керування видимістю)
Вся магія переміщення між меню та тестом тримається на одному CSS-класі — `.active`.
* **Як це працює:** Всі екрани (`menu`, `teststart`, `questionscreen`) спочатку приховані через `display: none`.
* **JS-логіка:** Коли користувач натискаємо кнопку, скрипт каже: *"Видали клас .active у старого екрана і додай його новому"*. Таким чином здійснюється перехід до наступного скріна.



## 2. Підготовка до гри (Вибір тесту)
Коли користувач натискає на кнопку тесту в меню:
1. Програма зчитує "секретний код" з атрибута `data-test` (наприклад, `math` або `iq`).
2. Цей код зберігається у змінну `selectedTest`.
3. Завдяки цьому "ключу" JS знає, які саме питання та заголовки витягнути з файлу з даними (`testdata.js`).

## 3. Головний ігровий цикл (loadQuestion)
Це серце гри. Кожне нове питання проходить 3 стадії:

1. **Очищення:** Команда `answers_wrap.innerHTML = ""` видаляє кнопки від попереднього питання, щоб звільнити місце.
2. **Генерація:** - Якщо обрано **Математику** — спрацьовує клас `RandomQuestion`, який придумує приклад на ходу.
   - Якщо **Тест** — береться готове питання з масиву.
3. **Відображення:** Через цикл `for` створюються нові кнопки. Кожна кнопка отримує свого "слухача" (`onclick`), який чекає на вибір відповіді.



## 4. Таймер та безпека
Для кожного питання вмикається функція `startTimer`.
* Якщо `timeLeft` дійде до 0 — гра негайно викликає функцію `finishTest`.
* **Важливо:** Як тільки натиснутьи на відповідь, ми викликаємо `clearInterval`, щоб зупинити таймер. Це потрібно, щоб час не закінчився, поки програється анімація переходу.

## 5. Фінал та розрахунки
Коли гра закінчується, запускається функція `finishTest`. Вона працює як калькулятор:
- Бере кількість набраних балів (`correctAnswers`).
- Ділить на номер останнього питання (`currentQuestionIndex`).
- Множить на 100, щоб отримати відсоток точності.
- Виводить результат і повертає на екран старту.